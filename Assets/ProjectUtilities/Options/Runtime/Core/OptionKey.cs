namespace ProjectUtilities.Options.Core
{
    /// <summary>
    /// Common option keys. Extend or replace this enum per project as needed.
    /// </summary>
    public enum OptionKey
    {
        MasterVolume = 0,
        MusicVolume = 1,
        SfxVolume = 2,
        GraphicsQuality = 3,
        Fullscreen = 4,
        ResolutionIndex = 5,
        MouseSensitivity = 6,
        InvertY = 7
    }
}

